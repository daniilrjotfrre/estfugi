// Example object
let object = {
    attr: attr$4
};

// This can be accessed as object.attr
// If the attribute name has special characters or is dynamic, use square bracket notation:
console.log(object['attr$4']);
